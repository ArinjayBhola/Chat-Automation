// Route marker for `/chat` (a new, blank chat). The actual chat UI is rendered
// by the persistent chat layout; ChatInterface reads the route to decide what
// to show, so this segment renders nothing itself.
export default function ChatPage() {
  return null;
}
