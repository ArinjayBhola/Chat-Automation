// Route marker for `/chat/[chatId]`. The persistent chat layout renders the UI
// and ChatInterface loads this chat from the `chatId` route param, so this
// segment renders nothing itself.
export default function ChatByIdPage() {
  return null;
}
